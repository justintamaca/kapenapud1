<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color for the spinner -->
    <item>
        <shape>
            <solid android:color="@color/white" /> <!-- Customize the background color -->
            <corners android:radius="12dp" /> <!-- Customize the corner radius -->
            <stroke android:color="@color/black" android:width="2dp" /> <!-- Add a border around the spinner -->
        </shape>
    </item>

</layer-list>